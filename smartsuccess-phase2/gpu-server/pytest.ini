[pytest]
# Real-GPU tests (faster-whisper inference on fixture WAVs) are excluded by
# default; run them on the GPU host with:  RUN_GPU_TESTS=1 pytest -m gpu
addopts = -m "not gpu"
markers =
    gpu: tests that need a CUDA GPU + faster-whisper (run manually on the GPU host)
asyncio_mode = auto
