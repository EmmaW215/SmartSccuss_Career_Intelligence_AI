[pytest]
# Default run excludes paid real-LLM tests; run them manually with:
#   pytest -m llm
addopts = -m "not llm"
markers =
    llm: integration tests that call a real LLM API (costs money, run manually)
