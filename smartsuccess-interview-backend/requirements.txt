# SmartSuccess Interview Backend - Python Dependencies
# FastAPI and Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Pydantic for data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# OpenAI and AI Services
openai>=1.10.0
httpx>=0.26.0

# Phase 2: Gemini API support (optional)
# google-generativeai>=0.3.0  # Uncomment if using Gemini

# Phase 2: Edge-TTS fallback (optional, free Microsoft TTS)
edge-tts>=6.1.10  # Free TTS fallback

# Environment and Configuration
python-dotenv>=1.0.0

# Async support
aiofiles>=23.2.1

# Numerical computing for embeddings
numpy>=1.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# Optional: Additional utilities
tenacity>=8.2.0  # Retry logic
structlog>=24.1.0  # Structured logging

# MatchWise module dependencies
stripe>=8.0.0  # Stripe payments
firebase-admin>=6.9.0  # Firebase Admin SDK
pdfplumber>=0.11.0  # PDF text extraction (replaces PyPDF2 for better multi-column support)
python-docx>=1.1.0  # DOCX text extraction
beautifulsoup4>=4.12.0  # HTML parsing
requests>=2.31.0  # HTTP requests
aiohttp>=3.9.0  # Async HTTP client

# Phase 1: LangGraph orchestration (feature-flagged for customize flow)
langchain>=0.3.0
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0

# Phase 2: Optional MCP integration
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0
